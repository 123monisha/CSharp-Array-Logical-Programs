/*using System;

namespace Basic_Programs
{
    internal class Max_Min_Find_Array
    {
        static void Main(string[] args)
        {
            int[] arr = { 3, 7, 9, 3, 0, 5 };

            int max = arr[0];
            int min = arr[0];

            foreach (int num in arr)
            {
                if (num > max)
                {
                    max = num;
                }
                else if (num < min)
                {
                    min = num;
                }
            }

            Console.WriteLine("Maximum: " + max);
            Console.WriteLine("Minimum: " + min);
        }
    }
}
*/